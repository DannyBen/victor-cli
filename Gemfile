source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'debug'
gem 'lp'
gem 'rspec'
gem 'rspec_approvals'
gem 'runfile'
gem 'runfile-tasks'
gem 'simplecov'

gemspec
