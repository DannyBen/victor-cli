source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'byebug'
gem 'rspec'
gem 'rspec_fixtures'
gem 'runfile'
gem 'runfile-tasks'
gem 'simplecov'

gemspec
